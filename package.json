{
  "name": "free_tailor",
  "private": true,
  "scripts": {
    "dev": "concurrently --names backend,frontend --prefix-colors cyan,magenta \"npm run dev --prefix backend\" \"npm run dev:live --prefix frontend\"",
    "dev:poll": "concurrently --names backend,frontend --prefix-colors cyan,magenta \"npm run dev:poll --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:live": "concurrently --names backend,frontend --prefix-colors cyan,magenta \"npm run dev --prefix backend\" \"npm run dev:live --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:backend:poll": "npm run dev:poll --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:frontend-live": "npm run dev:live --prefix frontend",
    "test": "npm run test --prefix backend",
    "test:backend": "npm run test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
